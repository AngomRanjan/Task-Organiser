/* || ============== Reset Functions  ============= || */

const resetTasks = () => {
  const ulTasks = document.getElementById('ul-tasks');
  ulTasks.innerHTML = '';
};

export default resetTasks;
/* || ============== Reset Functions Ends ============= || */
